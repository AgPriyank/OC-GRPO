# OC-GRPO — tested package versions (conda env, Python 3.10, CUDA 12.4, A100-40GB).
# Pinned versions are the exact ones used for the paper's runs; unpinned
# packages were installed at whatever version was compatible at the time.
#
# Install order matters: install torch first (with the cu124 index), then
# flash-attn (it compiles against the installed torch), then the rest.
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
#   pip install flash-attn --no-build-isolation
#   pip install -r requirements.txt
# Finally install the patched verl fork (see README, step 3):
#   cd verl && git apply ../verl_uncommitted.patch && pip install --no-deps -e . && cd ..

# --- core training stack (exact paper versions) ---
torch==2.6.0                # +cu124 build; install via the PyTorch cu124 index (see above)
vllm==0.8.5.post1
transformers==4.57.6
ray[default]==2.53.0
hydra-core==1.3.2
omegaconf==2.3.0
accelerate==1.12.0
pandas==2.3.3
pyarrow==18.1.0

# --- training stack (unpinned at run time) ---
numpy
peft
datasets
tensordict<=0.6.2         # ceiling from the verl fork's setup.py (installed with --no-deps)
codetiming
torchdata
# NOTE: the verl fork declares pyarrow>=19.0.0, intentionally overridden by the
# tested pyarrow==18.1.0 pin above (verl is installed with --no-deps).
# flash-attn             # install separately with --no-build-isolation (see above)

# --- answer grading (reward function + evaluation) ---
math-verify
sympy
pylatexenc
latex2sympy2_extended

# --- hint-generation clients (imported by verl_grpo.hints) ---
openai
requests
safetensors

# --- optional ---
wandb                    # only if you log with trainer.logger='[console,wandb]'
# anthropic              # only for frontier-hint runs (verl_run_858*): Claude API hints

tqdm
pyyaml
